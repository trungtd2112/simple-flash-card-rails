<%= form_with(model: card_review, local: true) do |form| %>
  <% if card_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(card_review.errors.count, "error") %> prohibited this card_review from being saved:</h2>

      <ul>
        <% card_review.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :card_id %>
    <%= form.number_field :card_id %>
  </div>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_area :review %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
